import { Router } from "express";
import { SYSTEM_LEVELS } from "../config/system.js";

const router = Router();

// Configuration endpoints have been consolidated into /api/auth/me

export default router;
